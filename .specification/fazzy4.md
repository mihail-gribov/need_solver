Давай зафиксируем **алгебру 4-значной нечёткой логики** аккуратно и формально, так чтобы её можно было дальше **реализовать, расширять и анализировать**.

Ниже — **чистая логика**, без объектов, рекомендаций и прикладного слоя.

---

## 1. Носитель логики

Каждое высказывание — это **двухкомпонентный вектор истинности**

[
x = (T_x, F_x), \qquad T_x, F_x \in [0,1]
]

* (T_x) — степень подтверждённости
* (F_x) — степень опровержения

Ограничений вида (T_x + F_x = 1) **нет**.

---

## 2. Базовые логические состояния

| Семантика        | Вектор |
| ---------------- | ------ |
| Истина (True)    | (1, 0) |
| Ложь (False)     | (0, 1) |
| Неизвестно (U)   | (0, 0) |
| Противоречие (C) | (1, 1) |

Это **четырёхзначная логика Белнапа**, но с непрерывными весами.

---

## 3. Используемая t-норма

В качестве базиса берем **операции Лукашевича**:

### T-норма (AND)

[
a \otimes b = \max(0, a + b - 1)
]

### S-норма (OR)

[
a \oplus b = \min(1, a + b)
]

### Отрицание (NOT)

[
\neg a = 1 - a
]

---

## 4. Отрицание формулы

Для вектора:

[
\neg (T, F) = (F, T)
]

Смысл:

* подтверждённость и опровержение меняются местами
* **противоречие и неизвестность сохраняются по форме**

---

## 5. Конъюнкция (AND)

Пусть:
[
x = (T_x, F_x), \quad y = (T_y, F_y)
]

Определяем:

### Истинность

[
T_{x \wedge y} = T_x \otimes T_y = \max(0, T_x + T_y - 1)
]

### Ложность

[
F_{x \wedge y} = F_x \oplus F_y = \min(1, F_x + F_y)
]

**Интерпретация:**

* чтобы утверждать AND, нужно подтверждение обоих
* чтобы опровергнуть AND, достаточно опровержения любого

---

## 6. Дизъюнкция (OR)

### Истинность

[
T_{x \vee y} = T_x \oplus T_y = \min(1, T_x + T_y)
]

### Ложность

[
F_{x \vee y} = F_x \otimes F_y = \max(0, F_x + F_y - 1)
]

**Интерпретация:**

* чтобы утверждать OR, достаточно одного
* чтобы опровергнуть OR, нужно опровергнуть оба

---

## 7. Импликация

Через стандартную конструкцию Лукашевича:

[
x \rightarrow y := \neg x \vee y
]

То есть:

[
T = \min(1, F_x + T_y)
]
[
F = \max(0, T_x + F_y - 1)
]

---

## 8. Нейтральные элементы

| Операция | Нейтральный элемент |
| -------- | ------------------- |
| AND      | (1, 0)              |
| OR       | (0, 1)              |
| NOT      | инволюция           |

---

## 9. Монотонность и корректность

✔ Все операции:

* замкнуты в ([0,1]^2)
* монотонны по соответствующим аргументам
* согласуются с булевой логикой при ({0,1})

---

## 10. Важное следствие

Эта алгебра:

* допускает **неполные знания**
* допускает **противоречивые знания**
* не «ломается» при конфликте
* идеально подходит для:

  * агрегации требований
  * композиции условий
  * постепенного уточнения информации


# Таблицы истинности

Отлично — зафиксируем **таблицы истинности** для канонических 4 значений
({T, F, U, C}), где:

* **T** = (1, 0)
* **F** = (0, 1)
* **U** = (0, 0)
* **C** = (1, 1)

Операции — ровно те, что мы определили: **двухкомпонентные (T,F)** + **Лукашевич**.

---

## 1. Отрицание (NOT)

[
\neg (T, F) = (F, T)
]

| x | ¬x |
| - | -- |
| T | F  |
| F | T  |
| U | U  |
| C | C  |

📌 Неизвестность и противоречие инвариантны.

---

## 2. Конъюнкция (AND, ∧)

[
\begin{aligned}
T &= \max(0, T_x + T_y - 1) \
F &= \min(1, F_x + F_y)
\end{aligned}
]

| x ∧ y | T | F | U | C |
| ----- | - | - | - | - |
| **T** | T | F | U | C |
| **F** | F | F | F | F |
| **U** | U | F | U | F |
| **C** | C | F | F | C |

Интуиция:

* F — поглощающий элемент
* C сохраняется только если оба несут подтверждение

---

## 3. Дизъюнкция (OR, ∨)

[
\begin{aligned}
T &= \min(1, T_x + T_y) \
F &= \max(0, F_x + F_y - 1)
\end{aligned}
]

| x ∨ y | T | F | U | C |
| ----- | - | - | - | - |
| **T** | T | T | T | T |
| **F** | T | F | U | C |
| **U** | T | U | U | T |
| **C** | T | C | T | C |

Интуиция:

* T — поглощающий элемент
* C возникает, если есть утверждение и отрицание

---

## 4. Импликация (→)

[
x \rightarrow y = \neg x \vee y
]

| x → y | T | F | U | C |
| ----- | - | - | - | - |
| **T** | T | F | U | C |
| **F** | T | T | T | T |
| **U** | T | U | U | T |
| **C** | T | C | U | C |

Интуиция:

* ложное основание ⇒ всегда истина
* противоречие в посылке **не убивает** следствие

---

## 5. Эквивалентность (↔) — полезная, производная

[
x \leftrightarrow y := (x \to y) \wedge (y \to x)
]

| x ↔ y | T | F | U | C |
| ----- | - | - | - | - |
| **T** | T | F | U | C |
| **F** | F | T | U | C |
| **U** | U | U | U | U |
| **C** | C | C | U | C |

# константы
TRUE        = (1, 0)
FALSE       = (0, 1)
UNDEF       = (0, 0)
CONFLICT    = (1, 1)
